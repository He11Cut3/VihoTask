@model IEnumerable<VUser>

    @foreach(var user in Model){
    <div>
        <a asp-controller="Chat" asp-action="CreatePrivateRoom" asp-route-userId="@user.Id">
            @user.UserName
        </a>
    </div>
    }